
DPATH       := $($(DOCKER_BASE_TRIXIE)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-base-trixie.mk rules/docker-base-trixie.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))
DEP_FILES   += dockers/dockerfile-macros.j2

$(DOCKER_BASE_TRIXIE)_CACHE_MODE  := GIT_CONTENT_SHA
$(DOCKER_BASE_TRIXIE)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST) INCLUDE_FIPS=$(INCLUDE_FIPS)
$(DOCKER_BASE_TRIXIE)_DEP_FILES   := $(DEP_FILES)
