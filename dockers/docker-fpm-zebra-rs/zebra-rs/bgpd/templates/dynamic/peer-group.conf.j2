{#- Nothing here, matching the FRR template: the dynamic family carries
    all of its group configuration in instance.conf.j2, because the group
    IS the per-session object. Kept as a file so the manager's template
    lookup finds it. -#}
